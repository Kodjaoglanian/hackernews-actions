name: Hacker News to Discord

on:
  schedule:
    - cron: "0 9 * * *"  # Executa todo dia às 09:00 UTC
  workflow_dispatch:

permissions:
  contents: read

env:
  NEWS_COUNT: 5           # Quantidade de notícias a enviar
  EMBED_COLOR: 16737843   # Cor laranja do Hacker News em decimal (#FF6600)
  TIMEZONE: "UTC"         # Fuso horário para o timestamp

jobs:
  send-news:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python 3.x
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install httpx pytz

      - name: Validate Discord webhook URL
        run: |
          if [ -z "${{ secrets.DISCORD_WEBHOOK_URL }}" ]; then
            echo "::error::❌ DISCORD_WEBHOOK_URL is not set. Exiting."
            exit 1
          fi
          echo "✅ Discord webhook URL found."

      - name: Send Hacker News Top Stories to Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          NEWS_COUNT: ${{ env.NEWS_COUNT }}
          EMBED_COLOR: ${{ env.EMBED_COLOR }}
          TIMEZONE: ${{ env.TIMEZONE }}
        run: |
          python << 'EOF'
          import os
          import sys
          import asyncio
          import httpx
          from datetime import datetime
          import pytz

          WEBHOOK_URL = os.getenv("DISCORD_WEBHOOK_URL")
          NEWS_COUNT = int(os.getenv("NEWS_COUNT", 5))
          EMBED_COLOR = int(os.getenv("EMBED_COLOR", 0xFF6600))
          TIMEZONE = os.getenv("TIMEZONE", "UTC")

          def log(msg):
              now = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
              print(f"[{now}] {msg}")

          async def fetch_json(client, url, retries=3):
              for attempt in range(1, retries + 1):
                  try:
                      resp = await client.get(url, timeout=10)
                      resp.raise_for_status()
                      return resp.json()
                  except Exception as e:
                      log(f"⚠️ Attempt {attempt} failed for {url}: {e}")
                      if attempt == retries:
                          return None
                      await asyncio.sleep(1)

          async def get_top_story_ids(client):
              url = "https://hacker-news.firebaseio.com/v0/topstories.json"
              return await fetch_json(client, url) or []

          async def get_story(client, story_id):
              url = f"https://hacker-news.firebaseio.com/v0/item/{story_id}.json"
              return await fetch_json(client, url)

          async def validate_webhook(client, url):
              try:
                  resp = await client.get(url, timeout=10)
                  if resp.status_code == 200:
                      return True
                  else:
                      log(f"❌ Webhook URL invalid or unreachable: Status {resp.status_code}")
                      return False
              except Exception as e:
                  log(f"❌ Error validating webhook URL: {e}")
                  return False

          def build_embed(stories):
              tz = pytz.timezone(TIMEZONE)
              current_time = datetime.now(tz).strftime("%d/%m/%Y %H:%M %Z")

              embed = {
                  "title": "📰 Hacker News — Top 5 do Dia",
                  "description": "Confira as principais notícias do dia direto do Hacker News. 🚀",
                  "url": "https://news.ycombinator.com/",
                  "color": EMBED_COLOR,
                  "footer": {
                      "text": f"Atualizado em {current_time} • Powered by GitHub Actions",
                      "icon_url": "https://news.ycombinator.com/favicon.ico"
                  },
                  "fields": []
              }

              for story in stories:
                  if not story:
                      continue
                  title = story.get("title", "Sem título")
                  url = story.get("url") or f"https://news.ycombinator.com/item?id={story.get('id')}"
                  author = story.get("by", "desconhecido")
                  score = story.get("score", "N/A")
                  comments = story.get("descendants", 0)
                  timestamp = story.get("time")
                  if timestamp:
                      pub_date = datetime.fromtimestamp(timestamp, tz).strftime("%d/%m/%Y %H:%M")
                  else:
                      pub_date = "Data desconhecida"

                  if len(title) > 256:
                      title = title[:253] + "..."

                  value = (
                      f"[🔗 Acessar notícia]({url})\n"
                      f"👤 Autor: **{author}**\n"
                      f"⭐ Pontos: **{score}** | 💬 Comentários: **{comments}**\n"
                      f"📅 Publicado em: {pub_date}"
                  )

                  embed["fields"].append({
                      "name": title,
                      "value": value,
                      "inline": False
                  })

              return embed

          async def send_discord(webhook_url, embed):
              async with httpx.AsyncClient() as client:
                  valid = await validate_webhook(client, webhook_url)
                  if not valid:
                      raise RuntimeError("Webhook inválido ou inacessível.")

                  payload = {"embeds": [embed]}
                  resp = await client.post(webhook_url, json=payload, timeout=10)
                  resp.raise_for_status()
                  log("✅ Mensagem enviada com sucesso para o Discord!")

          async def main():
              log("🔍 Iniciando coleta das notícias...")
              async with httpx.AsyncClient() as client:
                  top_ids = await get_top_story_ids(client)
                  if not top_ids:
                      log("⚠️ Nenhum ID de notícia retornado.")
                      return

                  tasks = [get_story(client, sid) for sid in top_ids[:NEWS_COUNT]]
                  stories = await asyncio.gather(*tasks)

              embed = build_embed(stories)
              await send_discord(WEBHOOK_URL, embed)

          if __name__ == "__main__":
              try:
                  asyncio.run(main())
              except Exception as e:
                  log(f"❌ Erro crítico: {e}")
                  sys.exit(1)
          EOF
